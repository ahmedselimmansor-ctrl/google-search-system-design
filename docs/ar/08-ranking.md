<div align="left">

**[🇬🇧 Read in English](../en/08-ranking.md)**

</div>

<div dir="rtl" align="right">

# الفصل ٠٨ · الترتيب والملاءمة

**← [٠٧ · الخدمة](07-serving.md)** · [🏠 الرئيسية](../../README.ar.md) · **[٠٩ · التخزين](09-storage.md) →**

---

## ٨.١ الترتيب هو المنتج

كل ما في الفصول ٠٤–٠٧ موجود لتسليم بضع مئات من المرشحين المعقولين إلى هذا الفصل. فالاسترجاع مسألة هندسية لها أجوبة صحيحة، أما **الترتيب فمسألة حُكم بلا حقيقة أساس** — إذ إن تحديد أي الوثائق «أفضل» لاستعلام مثل «جاكوار» يعتمد على من يسأل، ومتى، ولماذا.

والنتيجة المعمارية هي **التتالي**: طبّق تسجيلًا رخيصًا على وثائق كثيرة وتسجيلًا مكلفًا على وثائق قليلة، بحيث تبقى الكلفة الكلية داخل ميزانية الزمن بينما ينال الترتيب النهائي منفعة أقوى نموذج تستطيع تحمّله.

> **المخطط D-39 — قمع الاسترجاع والترتيب**

</div>

```mermaid
flowchart TB
    C0["🌐 المتن<br/>10¹¹ وثيقة"]

    C0 -->|"بحث في الفهرس المقلوب<br/>الكلفة: ~0"| C1["📑 المجموعة المطابِقة للمصطلحات<br/>10⁶ إلى 10⁹ وثيقة<br/>لا تُجسَّد أبدًا"]

    C1 -->|"تقليم BlockMax-WAND<br/>أفضل k دقيقة بلا خسارة جودة"| C2["⚡ L0 · المسترجَعة<br/>~10⁵ وثيقة<br/>تُلمس لكل شظية"]

    C2 -->|"L1: BM25 + إشارات ساكنة<br/>~10 نانو ثانية لكل وثيقة<br/>يعمل على خادم الأوراق"| C3["🔢 L1 · المُسجَّلة بثمن بخس<br/>~10³ لكل شظية<br/>← 10³ عالميًا بعد الدمج"]

    C3 -->|"L2: أشجار معزَّزة بالتدرج<br/>مئات السمات<br/>~10 ميكرو ثانية لكل وثيقة"| C4["🌲 L2 · المُسجَّلة بالسمات<br/>~10² وثيقة"]

    C4 -->|"L3: محوّل مُشفِّر تقاطعي<br/>انتباه كامل بين الاستعلام والوثيقة<br/>~1 مللي ثانية لكل وثيقة"| C5["🧠 L3 · المُعاد ترتيبها عصبيًا<br/>~10 وثائق"]

    C5 -->|"تنوع · حداثة ·<br/>إزالة تكرار المضيفين · مرشّحات السياسة"| C6["✅ صفحة النتائج النهائية<br/>10 نتائج"]

    subgraph BUDGET["انضباط الكلفة — جوهر التتالي كله"]
        B1["L1: 10⁵ وثيقة × 10 نانو  =   1 مللي ثانية"]
        B2["L2: 10³ وثيقة × 10 ميكرو =  10 مللي ثانية"]
        B3["L3: 10² وثيقة × 1 مللي   = 100 مللي ثانية"]
        B4["اقلب نسبة أي مرحلة<br/>تنفجر الميزانية"]
    end

    classDef wide fill:#dbeafe,stroke:#1e40af,color:#1c1917
    classDef mid fill:#fde68a,stroke:#b45309,color:#1c1917
    classDef narrow fill:#fecaca,stroke:#b91c1c,color:#1c1917
    classDef final fill:#bbf7d0,stroke:#15803d,color:#1c1917
    class C0,C1,C2 wide
    class C3,C4 mid
    class C5 narrow
    class C6 final
```

<div dir="rtl" align="right">

**اقرأ صندوق الميزانية بعناية — فهو البصيرة الجوهرية.** إن L3 أغلى من L1 بمئة ألف ضعف لكل وثيقة. وتشغيل L3 على 10⁵ وثيقة سيستغرق ١٠٠ ثانية. فالقمع ليس تحسينًا يُضاف فوق الترتيب، بل **القمع هو ما يجعل الترتيب المكلف ممكنًا أصلًا**.

والخطر المقابل: **الوثيقة التي تُسقط خطأً في L1 لا يمكن استعادتها أبدًا في L3.** فأخطاء الاستدعاء في بداية التتالي دائمة. ولهذا يكون L1 سخيًا عمدًا — إذ يُحسِّن الاستدعاء لا الدقة، ويترك الدقة للمراحل القادرة على تحمّلها.

---

## ٨.٢ تصنيف الإشارات

> **المخطط D-40 — من أين تأتي إشارات الترتيب**

</div>

```mermaid
mindmap
  root((إشارات<br/>الترتيب))
    معتمدة على الاستعلام
      تُحسب وقت الاستعلام
      تردد المصطلح في الوثيقة
      تردد الوثائق العكسي
      BM25 وتنويعاته الموزونة بالحقول
      تقارب المصطلحات
      تطابق العبارة والترتيب
      تطابقات الحقول
        تطابق العنوان
        تطابق الرابط
        تطابق نص الرابط
        تطابق الترويسة
      التشابه الدلالي
        جيب تمام المتجهات الكثيفة
        ملاءمة المشفّر التقاطعي
      تغطية الاستعلام
        نسبة المصطلحات المطابَقة
        حضور المصطلح الجوهري
    مستقلة عن الاستعلام
      تُحسب مسبقًا وقت الفهرسة
      قائمة على الروابط
        بيج رانك
        سلطة المضيف
        مسافة الثقة عن البذور
      جودة المحتوى
        الأصالة
        العمق والاكتمال
        درجة مصنّف السبام
      إشارات المستخدمين المجمّعة
        معدل النقر التاريخي
        معدل النقرة الطويلة
        معدل الارتداد
      تجربة الصفحة
        أداء التحميل
        الملاءمة للجوال
        استقرار التخطيط
        بروتوكول آمن
      الحداثة
        تاريخ النشر
        آخر تحديث جوهري
        معدل التغير
    معتمدة على السياق
      تُحسم لكل طلب
      الموقع
        الدولة والمنطقة
        المدينة للنية المحلية
      اللغة والمحلية
      الجهاز والشاشة
      الزمن
        وقت اليوم
        الموسمية
        الأحداث العاجلة
      التخصيص بموافقة
        سجل البحث
        التفضيلات الصريحة
```

<div dir="rtl" align="right">

والانقسام مهم عمليًا:

| الصنف | متى يُحسب | كلفة التخزين | كلفة الزمن |
|---|---|---|---|
| مستقل عن الاستعلام | دون اتصال، وقت الفهرسة | عالية — يُخزَّن لكل وثيقة | ~صفر وقت الاستعلام |
| معتمد على الاستعلام | وقت الاستعلام، في الورقة | ~صفر | التكلفة المهيمنة في الخدمة |
| معتمد على السياق | وقت الاستعلام، من الطلب | ~صفر | منخفضة، لكنها تضاعف مفاتيح الذاكرة المؤقتة |

**ادفع كل ما تستطيع إلى العمود المستقل عن الاستعلام.** وهذا هو المبدأ المتكرر من [الفصل ٠١](01-overview.md) — أنجز العمل المكلف دون اتصال. فمسار الاستعلام ينبغي أن **يجمع** أرقامًا محسوبة مسبقًا لا أن يحسبها.

---

## ٨.٣ بيج رانك — إشارة السلطة المستقلة عن الاستعلام

يُنمذج بيج رانك متصفحًا عشوائيًا يتبع الروابط عشوائيًا ويقفز أحيانًا إلى صفحة عشوائية. والاحتمال المستقر لوجوده في الصفحة *p* هو قيمة بيج رانك لها.

$$PR(p) = \frac{1-d}{N} + d \sum_{q \in In(p)} \frac{PR(q)}{L(q)}$$

حيث *d* ≈ 0.85 معامل التخميد، و*N* عدد الصفحات، و*In(p)* مجموعة الصفحات المرتبطة بـ*p*، و*L(q)* درجة خروج *q*.

> **المخطط D-41 — حساب بيج رانك عند الحجم الضخم**

</div>

```mermaid
flowchart TB
    G[("بيان الروابط<br/>10¹¹ عقدة · 10¹² حافة<br/>أكبر بكثير من آلة واحدة")]

    G --> INIT["التهيئة<br/>PR لكل صفحة = 1/N"]

    INIT --> ITER["التكرار k"]

    subgraph MR["تكرار واحد = عملية MapReduce واحدة"]
        MAP["الخريطة<br/>لكل صفحة q بقيمة PR:<br/>أصدر ⟨p، PR(q)/L(q)⟩ لكل رابط صادر<br/>وأصدر أيضًا قائمة الجوار"]
        SHUF["الخلط<br/>تجميع المساهمات حسب الصفحة الهدف"]
        RED["الاختزال<br/>PR(p) = (1−d)/N + d × مجموع المساهمات<br/>وإعادة إصدار قائمة الجوار"]
        MAP --> SHUF --> RED
    end

    ITER --> MAP
    RED --> CONV{"هل تقارب؟<br/>مجموع الفروق أقل من إبسلون<br/>عادةً 20 إلى 50 تكرارًا"}

    CONV -->|"لا"| ITER
    CONV -->|"نعم"| POST["معالجة لاحقة"]

    POST --> P1["معالجة العقد المتدلية<br/>الصفحات بلا روابط صادرة<br/>تُعاد كتلتها بالتساوي"]
    POST --> P2["تحويل لوغاريتمي وتقسيم إلى دلاء<br/>عدد صحيح صغير<br/>— الترتيب النسبي وحده يهم"]
    POST --> P3["حساب تجميعات على مستوى<br/>المضيف والنطاق"]

    P1 & P2 & P3 --> OUT[("درجة سلطة لكل وثيقة<br/>← تُخزَّن في مرفقات الوثيقة<br/>← تُشحن مع الفهرس")]

    subgraph VARIANTS["تنويعات عملية"]
        V1["بيج رانك الحساس للموضوع:<br/>متجهات منفصلة لكل موضوع<br/>تُدمج حسب موضوع الاستعلام"]
        V2["ترست رانك:<br/>بذور من مواقع جيدة مفحوصة يدويًا<br/>وتنتشر الثقة لمحاربة السبام"]
        V3["مخمَّد بجودة الروابط:<br/>خصم روابط nofollow<br/>والمدفوعة وحواف مزارع الروابط"]
    end

    OUT --> VARIANTS

    classDef compute fill:#bfdbfe,stroke:#1d4ed8,color:#1c1917
    classDef store fill:#e9d5ff,stroke:#7e22ce,color:#1c1917
    class MAP,SHUF,RED,INIT compute
    class G,OUT store
```

<div dir="rtl" align="right">

**تحفظان صادقان.**

أولًا، **بيج رانك إشارة واحدة بين مئات**، وقد انخفض وزنه النسبي انخفاضًا هائلًا منذ ١٩٩٨. واعتباره «خوارزمية جوجل» خطأ شائع وجسيم. فهو يوفّر افتراضًا مسبقًا عن السلطة مستقلًا عن الاستعلام، ولا يقول شيئًا عمّا إذا كانت الصفحة تجيب عن السؤال.

ثانيًا، **العقد المتدلية ليست حاشية.** فنسبة كبيرة من الويب بلا روابط صادرة (ملفات PDF والصور والصفحات الطرفية). وبلا إعادة توزيع كتلة رتبتها، يتسرّب الاحتمال خارج النظام في كل تكرار ولا يتقارب الحساب إلى توزيع سليم. فمعالجتها بصورة صحيحة متطلب تنفيذي حقيقي لا لباقة نظرية.

---

## ٨.٤ التعلم للترتيب

ضبط مئات أوزان السمات يدويًا مستحيل. فالترتيب يُتعلَّم من بيانات موسومة.

> **المخطط D-42 — حلقة تدريب ونشر التعلم للترتيب**

</div>

```mermaid
flowchart TB
    subgraph LABELS["① الحصول على التسميات"]
        HR["محكّمون بشر<br/>أحكام مقادة بإرشادات<br/>ملاءمة متدرجة من 0 إلى 4<br/>مكلفة وغير متحيزة"]
        CL["سجلات النقر<br/>رخيصة ووفيرة<br/>متحيزة بشدة للموضع"]
        DEBIAS["تصحيح التحيز<br/>ترجيح بمعكوس الميل<br/>ونموذج تحيز الموضع<br/>وتجارب العشوائية"]
        CL --> DEBIAS
    end

    subgraph FEATS["② استخراج السمات"]
        FX["تسجيل متجهات سمات الاستعلام والوثيقة<br/>كما تُحسب في الإنتاج تمامًا<br/>⚠️ انحراف التدريب عن الخدمة<br/>هو السبب الأول للفشل الصامت"]
    end

    HR & DEBIAS --> FX

    subgraph TRAIN["③ تدريب النموذج"]
        OBJ{"صياغة الهدف"}
        OBJ -->|"نقطية"| PW["تنبؤ الملاءمة لكل وثيقة<br/>بسيط ويتجاهل بنية الترتيب"]
        OBJ -->|"زوجية"| PR2["تنبؤ أي وثيقتين أعلى ترتيبًا<br/>— RankNet"]
        OBJ -->|"قائمية"| LW["تحسين القائمة كلها<br/>مباشرةً مقابل NDCG —<br/>LambdaMART"]
        LW --> MODEL["النموذج المُدرَّب<br/>أشجار لـL2 · محوّل لـL3"]
        PW --> MODEL
        PR2 --> MODEL
    end

    FX --> OBJ

    subgraph EVAL["④ التقييم دون اتصال"]
        E1["NDCG@10 على استعلامات<br/>محجوزة مُقيَّمة بشريًا"]
        E2["تحليل الشرائح:<br/>حسب اللغة · المحلية ·<br/>طول الاستعلام · النية · الرأس والذيل"]
        E3["تقدير مضاد للواقع<br/>من البيانات المسجَّلة"]
    end

    MODEL --> E1 & E2 & E3

    E1 & E2 & E3 --> GATE{"هل يتفوق على الإنتاج<br/>في كل شريحة؟"}
    GATE -->|"لا"| BACK["كرّر — وحقّق في<br/>الشريحة المنحدرة"]
    BACK --> OBJ

    GATE -->|"نعم"| AB["⑤ اختبار A/B مباشر<br/>على شريحة مرور صغيرة<br/>راجع الفصل ١٣"]

    AB --> METRICS{"المقاييس المباشرة"}
    METRICS --> M1["تقييم بشري جنبًا إلى جنب ⬆"]
    METRICS --> M2["معدل النقرة الطويلة ⬆"]
    METRICS --> M3["إعادة صياغة الاستعلام ⬇"]
    METRICS --> M4["الزمن ضمن الهدف"]

    M1 & M2 & M3 & M4 --> SHIP{"كلها خضراء؟"}
    SHIP -->|"نعم"| RAMP["تصعيد 1٪ ← 10٪ ← 50٪ ← 100٪"]
    SHIP -->|"لا"| ROLL["🛑 تراجع وحلّل"]

    RAMP --> PROD(("ترتيب الإنتاج"))
    PROD -.->|"يولّد سجلات جديدة"| CL

    classDef human fill:#bbf7d0,stroke:#15803d,color:#1c1917
    classDef risky fill:#fecaca,stroke:#b91c1c,color:#1c1917
    classDef ml fill:#bfdbfe,stroke:#1d4ed8,color:#1c1917
    class HR,M1 human
    class CL,FX risky
    class PW,PR2,LW,MODEL ml
```

<div dir="rtl" align="right">

### لماذا تفوز الأهداف القائمية

تُقاس جودة الترتيب بـNDCG، وهي خاصية **القائمة كلها** لا خاصية الوثائق منفردة. فالنموذج النقطي الذي يتنبأ بملاءمة كل وثيقة تنبؤًا مثاليًا بمعزل عن غيرها قد ينتج مع ذلك قائمة رديئة، لأنه لا يدرك أن تبديل الموضعين الأول والثاني أهم بكثير من تبديل التاسع والعاشر. أما LambdaMART فيعمل بترجيح كل زوج تدريبي مباشرةً بـ**مقدار تغيّر NDCG** لو تبادلت الوثيقتان موضعيهما — وهذا يوائم التدرج مع المقياس الذي يهمك فعلًا.

### نمطا الفشل اللذان يعضّان فعلًا

**تحيز الموضع.** ينقر المستخدمون على النتيجة الأولى أكثر بكثير من الخامسة بصرف النظر عن الملاءمة. والنموذج المُدرَّب ساذجًا على النقرات يتعلم أن «الوثائق التي رُتِّبت أولًا جيدة»، وهذا استدلال دائري: فهو يرسّخ الترتيب الحالي ولا يستطيع أبدًا اكتشاف أن السابعة كانت أفضل. والحلول — الترجيح بمعكوس الميل، وعشوَنة النتائج عمدًا لشريحة مرور صغيرة لجمع بيانات غير متحيزة — إلزامية لا تحسينات اختيارية.

**انحراف التدريب عن الخدمة.** إن حُسبت سمةٌ بطريقة في خط التدريب وبطريقة مختلفة قليلًا في ثنائية الخدمة، تلقّى النموذج مدخلات لم يُدرَّب عليها قط. وهذا لا ينتج خطأً ولا تنبيهًا؛ بل تتدهور الجودة فحسب. والدفاع الموثوق الوحيد هو **تسجيل السمات من خدمة الإنتاج والتدريب على تلك المتجهات المسجَّلة بعينها**، لا إعادة حسابها في خط التدريب.

---

## ٨.٥ الاسترجاع العصبي — أبعد من مطابقة المصطلحات

يفشل الاسترجاع اللفظي في **مشكلة عدم تطابق المفردات**: فوثيقة عن «صيانة المركبات» لا تطابق استعلامًا عن «إصلاح السيارات» رغم أنها بالضبط ما يريده المستخدم. ويحل الاسترجاع الكثيف ذلك بإسقاط الاستعلامات والوثائق في فضاء متجهي دلالي مشترك.

> **المخطط D-43 — الاسترجاع الكثيف والدمج الهجين**

</div>

```mermaid
flowchart TB
    subgraph OFFLINE["دون اتصال — وقت الفهرسة"]
        D["نص الوثيقة"] --> DE["مُشفِّر الوثائق<br/>محوّل ثنائي المشفّر"]
        DE --> DV["متجه كثيف<br/>768 بُعدًا مكمَّم إلى int8"]
        DV --> ANN["بناء فهرس الجيران التقريبيين"]
        ANN --> A1["HSNW: بيان عالم صغير قابل للملاحة<br/>استدعاء ممتاز وذاكرة عالية"]
        ANN --> A2["IVF-PQ: عنقدة خشنة +<br/>تكميم جدائي<br/>ذاكرة أقل واستدعاء قابل للضبط"]
    end

    subgraph ONLINE["مباشر — وقت الاستعلام"]
        Q["نص الاستعلام"] --> QE["مُشفِّر الاستعلام<br/>فضاء التضمين نفسه"]
        QE --> QV["متجه الاستعلام"]
    end

    QV --> SEARCH["بحث الجيران التقريبيين<br/>~1 إلى 5 مللي ثانية لأفضل 1000"]
    A1 & A2 --> SEARCH

    subgraph HYBRID["الاسترجاع الهجين — كلا المسارين يعملان"]
        LEX["🔤 مرشحون لفظيون<br/>BM25 / الفهرس المقلوب<br/>✅ المصطلحات الدقيقة والأسماء والأرقام<br/>✅ قابل للتفسير ورخيص<br/>❌ عدم تطابق المفردات"]
        SEM["🧠 مرشحون دلاليون<br/>بحث المتجهات الكثيفة<br/>✅ المرادفات وإعادة الصياغة وعبور اللغات<br/>❌ ضعيف على السلاسل النادرة الدقيقة<br/>❌ معتم ومكلف التحديث"]
    end

    SEARCH --> SEM
    LEXIN["الفهرس المقلوب<br/>الفصل ٠٦"] --> LEX

    LEX & SEM --> FUSE["الدمج"]
    FUSE --> F1["دمج الرتب المتبادل:<br/>الدرجة = مجموع 1/(k + الرتبة)<br/>بلا حاجة لمعايرة الدرجات"]
    FUSE --> F2["دمج متعلَّم:<br/>كلتا الدرجتين كسمتين في L2"]

    F1 & F2 --> CAND["مجموعة المرشحين المدموجة"]
    CAND --> XE["إعادة ترتيب بالمشفّر التقاطعي<br/>يُقرأ الاستعلام والوثيقة معًا<br/>بانتباه كامل بينهما<br/>أدق بكثير وأغلى بكثير"]
    XE --> FINAL(("الترتيب النهائي"))

    classDef lex fill:#fde68a,stroke:#b45309,color:#1c1917
    classDef sem fill:#e9d5ff,stroke:#7e22ce,color:#1c1917
    classDef fuse fill:#bbf7d0,stroke:#15803d,color:#1c1917
    class LEX,LEXIN lex
    class SEM,DE,QE,DV,QV,SEARCH sem
    class FUSE,F1,F2,CAND fuse
```

<div dir="rtl" align="right">

### المشفّر الثنائي مقابل التقاطعي — لماذا يوجد كلاهما

| | **الثنائي** (للاسترجاع) | **التقاطعي** (لإعادة الترتيب) |
|---|---|---|
| البنية | يشفّر الاستعلام والوثيقة **منفصلين** | يشفّرهما **معًا** |
| متجهات الوثائق | تُحسب مسبقًا وتُخزَّن في الفهرس | يجب حسابها لكل زوج استعلام‑وثيقة |
| الكلفة وقت الاستعلام | تشفير واحد للاستعلام + بحث تقريبي | مرور محوّل كامل **لكل وثيقة** |
| الدقة | جيدة | أفضل بكثير |
| قابل للتطبيق على | 10¹¹ وثيقة | ~10² وثيقة |

وهذا بالضبط سبب وجود التتالي. فالمشفّر الثنائي يستطيع حساب متجهات الوثائق مسبقًا لأنه لا ينظر إلى الاستعلام أثناء التشفير — فيصير الاسترجاع عبر المليارات ممكنًا. والمشفّر التقاطعي أدق **لأنه بالتحديد** يقرأهما معًا، وهو أيضًا سبب استحالة حسابه مسبقًا. فالرخيص المتوازي يسترجع، والمكلف الدقيق يعيد الترتيب.

### متى يفوز الاسترجاع اللفظي رغم ذلك

الاسترجاع الكثيف ليس بديلًا؛ فهو يخسر باطّراد في:

- **المعرّفات الدقيقة** — أرقام القطع ورموز الأخطاء وأسماء الاستثناءات وأرقام الهواتف
- **أسماء العلم النادرة** الغائبة أو ضعيفة التمثيل في بيانات التدريب
- **النفي والمنطق الدقيق** — إذ تُشتهر التضمينات بطمس الفرق بين «س» و«ليس س»
- **الحداثة** — فالكيان الجديد تمامًا ليس في مفردات نموذج التضمين حتى يُعاد تدريبه، بينما يملكه الفهرس المقلوب لحظة زحفه

والنقطة الأخيرة مهمة معماريًا: **الفهرس اللفظي يتحدّث خلال دقائق، ونموذج التضمين يتحدّث خلال أسابيع.** فالاسترجاع الهجين ليس تحوّطًا، بل هو دمج نظامين بنمطَي فشل متكاملين حقًا.

---

## ٨.٦ التخصيص والسياق

> **المخطط D-44 — تطبيق السياق في الترتيب**

</div>

```mermaid
flowchart TB
    BASE["الترتيب الأساسي<br/>من L2 و L3"]

    BASE --> CTX{"تطبيق السياق"}

    CTX --> LOC["📍 الموقع"]
    LOC --> L1["الدولة ← انحياز المتن والمحلية"]
    LOC --> L2["المدينة ← نتائج النية المحلية"]
    LOC --> L3["⚠️ قانوني: إزالات محدودة بالمنطقة"]

    CTX --> LANG["🗣️ اللغة"]
    LANG --> LG1["لغة الواجهة"]
    LANG --> LG2["تفضيل لغة المحتوى"]
    LANG --> LG3["الاسترجاع عبر اللغات حين<br/>يكون المتن ضعيفًا باللغة الأصلية"]

    CTX --> TIME["🕐 الزمن"]
    TIME --> T1["تعزيز الحداثة للنية الإخبارية"]
    TIME --> T2["الموسمية والأحداث المتكررة"]
    TIME --> T3["كشف الأحداث العاجلة ←<br/>رفع وزن الحداثة مؤقتًا"]

    CTX --> DEV["📱 الجهاز"]
    DEV --> D1["ترجيح الملاءمة للجوال"]
    DEV --> D2["عدد النتائج والتخطيط"]

    CTX --> HIST["👤 السجل بموافقة"]
    HIST --> H1["سياق الجلسة:<br/>الاستعلام السابق يفض التباس<br/>'جاكوار' بعد 'تذاكر حديقة الحيوان'"]
    HIST --> H2["الاهتمامات طويلة المدى<br/>— بوزن صغير مع احترام الانسحاب"]

    L1 & L2 & L3 & LG1 & LG2 & LG3 & T1 & T2 & T3 & D1 & D2 & H1 & H2 --> ADJ["الترتيب المعدَّل"]

    ADJ --> GUARD["حواجز الحماية"]
    GUARD --> G1["تحديد سقف تأثير التخصيص<br/>— فهو فاصل تعادل لا مُعيد ترتيب"]
    GUARD --> G2["الحفاظ على التنوع<br/>وتجنّب فقاعات الترشيح"]
    GUARD --> G3["لا تُخصِّص أبدًا بعيدًا عن<br/>النتائج الموثوقة في مواضيع<br/>الصحة والسلامة والشأن العام"]
    GUARD --> G4["الانسحاب الكامل يجب أن يُحترم<br/>وأن يعمل فعلًا"]

    G1 & G2 & G3 & G4 --> FINAL(("صفحة نتائج مخصَّصة"))

    classDef ctx fill:#bfdbfe,stroke:#1d4ed8,color:#1c1917
    classDef guard fill:#fecaca,stroke:#b91c1c,color:#1c1917
    class LOC,LANG,TIME,DEV,HIST ctx
    class GUARD,G1,G2,G3,G4 guard
```

<div dir="rtl" align="right">

**التخصيص مكلف معماريًا بطريقة يسهل إغفالها.** فكل بُعد سياقي تضيفه يضاعف فضاء مفاتيح ذاكرة النتائج المؤقتة. فذاكرة مفتاحها `(استعلام، دولة، لغة)` قد تصيب ٤٠٪ من الوقت، أما مفتاحها `(استعلام، دولة، لغة، مدينة، جهاز، معرّف مستخدم)` فلن يصيب عمليًا أبدًا. ولما كان [الفصل ٠٣](03-capacity-estimation.md) قد بيّن أن الذاكرة المؤقتة تزيل ~٤٠٪ من تكلفة الخدمة، فإن التخصيص العدواني قد **يضاعف تكلفة أسطول الخدمة بأكمله**. وهذا — أكثر بكثير من أي حجة خصوصية — هو سبب تطبيق التخصيص الإنتاجي كتعديل خفيف بعد الترتيب لا كاسترجاع لكل مستخدم.

---

## ٨.٧ التنوع والتعديلات النهائية

أفضل عشرة بالدرجة غالبًا ما تكون صفحة نتائج رديئة. فعشر صفحات شبه متطابقة من موقع واحد تُعظّم الملاءمة تقنيًا بينما تخدم المستخدم خدمة سيئة.

> **المخطط D-45 — تحسين مجموعة النتائج بعد الترتيب**

</div>

```mermaid
flowchart LR
    IN["أفضل ~50 بالدرجة"] --> DIV["التنويع"]

    DIV --> D1["عنقدة المضيفين<br/>نتيجتان كحد أقصى لكل نطاق<br/>وطيّ الباقي تحت<br/>'مزيد من النتائج من…'"]
    D1 --> D2["تغطية النوايا<br/>استعلام ملتبس مثل 'جاكوار' ←<br/>غطِّ الحيوان والسيارة والفريق"]
    D2 --> D3["اختيار بأسلوب MMR<br/>الدرجة ناقص لامدا × أقصى تشابه<br/>مع النتائج المختارة سلفًا"]
    D3 --> D4["مزج أنواع المحتوى<br/>تجنّب عشرة مواضيع منتديات"]

    D4 --> FRESH["تعديل الحداثة<br/>عزّز الأحدث إن كانت النية إخبارية<br/>أو كان الاستعلام يستحق الحداثة"]

    FRESH --> FILT["المرشّحات النهائية"]
    FILT --> F1["البحث الآمن"]
    FILT --> F2["الإزالات القانونية محدودة المنطقة"]
    FILT --> F3["عتبة السبام"]
    FILT --> F4["كبح الروابط المكسورة"]

    F1 & F2 & F3 & F4 --> WHY["تعليقات قابلية التفسير<br/>لماذا تحتل هذه النتيجة هذا الموضع<br/>— للتصحيح وللمحكّمين"]

    WHY --> OUT(("العشرة النهائية"))

    classDef div fill:#fde68a,stroke:#b45309,color:#1c1917
    classDef filt fill:#fecaca,stroke:#b91c1c,color:#1c1917
    class D1,D2,D3,D4 div
    class F1,F2,F3,F4 filt
```

<div dir="rtl" align="right">

**التنويع مقايضة صريحة للملاءمة المقيسة بالنفع الفعلي.** فكل قيد تنويع **يخفض** قيمة NDCG للقائمة محسوبةً مقابل تسميات ملاءمة كل وثيقة، لأنك تمتنع عمدًا عن عرض الوثيقة الأعلى درجة في موضع ما. ولا يُبرَّر ذلك إلا بمقاييس على مستوى المستخدم — نجاح المهمة، ومعدل إعادة الصياغة، والنقرات الطويلة — وهو مثال جيد على سبب إصرار [الفصل ٠٢](02-requirements.md) على مقاييس جودة متعددة ومتعارضة بدل هدف تحسين واحد.

---

## ٨.٨ الملخص — كومة الترتيب

| المرحلة | تعمل على | الوثائق داخلًا ← خارجًا | النموذج | الزمن |
|---|---|---:|---|---:|
| **L0 الاسترجاع** | الورقة | 10¹¹ ← 10⁵ | فهرس مقلوب + WAND | ~40 مللي ثانية |
| **L1 التسجيل الرخيص** | الورقة | 10⁵ ← 10³ | BM25 + إشارات ساكنة | ضمن الأربعين |
| **L2 التسجيل بالسمات** | طبقة الترتيب | 10³ ← 10² | أشجار معزَّزة بالتدرج | ~30 مللي ثانية |
| **L3 إعادة الترتيب العصبي** | مسرّعات | 10² ← 10¹ | محوّل مشفّر تقاطعي | ~50 مللي ثانية |
| **المعالجة اللاحقة** | الخالط | 10¹ ← 10¹ | قواعد وتنويع | ~5 مللي ثانية |

</div>

<div align="center">

**← [٠٧ · الخدمة](07-serving.md)** · [🏠 الرئيسية](../../README.ar.md) · **[٠٩ · التخزين](09-storage.md) →** · [🇬🇧 English](../en/08-ranking.md)

</div>
