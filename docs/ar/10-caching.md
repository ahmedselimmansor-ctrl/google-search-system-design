<div align="left">

**[🇬🇧 Read in English](../en/10-caching.md)**

</div>

<div dir="rtl" align="right">

# الفصل ١٠ · استراتيجية التخزين المؤقت

**← [٠٩ · التخزين](09-storage.md)** · [🏠 الرئيسية](../../README.ar.md) · **[١١ · الحداثة](11-freshness.md) →**

---

## ١٠.١ لماذا التخزين المؤقت قرار اقتصادي لا قرار زمن

الغريزة تقول إن الذاكرات المؤقتة تجعل الأمور سريعة. وفي هذا النظام تجعلها **رخيصة** في المقام الأول، والفارق يغيّر طريقة تصميمها.

من [الفصل ٠٣](03-capacity-estimation.md): مرور الذروة 3 × 10⁵ استعلام/ثانية، ونسبة إصابة ٤٠٪ في ذاكرة النتائج تعني أن 1.8 × 10⁵ فقط تصل إلى الفهرس. وبدون تلك الذاكرة سيحتاج أسطول الخدمة إلى أن يكون أكبر بنحو ٦٧٪. فالذاكرة المؤقتة لا تقتطع مللي ثوانٍ من مسار سريع أصلًا، بل تحذف ثلثَي عتادِ أسطولٍ كامل.

وهذا يعيد صياغة سؤال التصميم. فهو ليس «كيف نجعل الاستعلامات المخزّنة أسرع؟» بل **«كيف نعظّم نسبة المرور الذي لا يلمس الفهرس أبدًا، دون أن نقدّم شيئًا خاطئًا قط؟»**

---

## ١٠.٢ مرور الاستعلامات شديد الانحراف، وهذا ما يجعل التخزين المؤقت ناجحًا

> **المخطط D-51 · توزيع شعبية الاستعلامات وقابلية التخزين**

</div>

```mermaid
flowchart TB
    ALL["كل الاستعلامات<br/>~10⁵ في الثانية متوسطًا"]

    ALL --> HEAD["🔥 الرأس<br/>~10³ استعلام متمايز<br/>≈ 15-20٪ من المرور<br/>'فيسبوك' · 'الطقس' · 'يوتيوب'"]
    ALL --> TORSO["📊 الجذع<br/>~10⁷ استعلام متمايز<br/>≈ 40-50٪ من المرور<br/>شائعة لكن غير كونية"]
    ALL --> TAIL["🌊 الذيل<br/>~10⁹⁺ استعلام متمايز<br/>≈ 30-40٪ من المرور<br/>15٪ من استعلامات اليوم<br/>سلاسل لم تُرَ من قبل"]

    HEAD --> HC["الذاكرة: ✅ إصابة شبه دائمة<br/>بصمة ذاكرة ضئيلة<br/>نسبة الإصابة ~99٪"]
    TORSO --> TC["الذاكرة: ✅ نسبة إصابة جيدة<br/>هنا تعيش معظم قيمة الذاكرة<br/>نسبة الإصابة ~50٪"]
    TAIL --> TLC["الذاكرة: ❌ إصابة تكاد تنعدم<br/>تخزينه يهدر الذاكرة<br/>نسبة الإصابة ~2٪"]

    HC & TC --> SAVE["إصابة مجمّعة ~40٪<br/>= إزالة ~40٪ من تكلفة الخدمة"]
    TLC --> FULL["استعلامات الذيل تدفع دائمًا<br/>كلفة الاسترجاع والترتيب كاملة<br/>← وعلى أساسها يُحجَّم الأسطول"]

    subgraph INSIGHT["النتيجة التصميمية"]
        I1["ينبغي أن تستهدف سعة الذاكرة الجذعَ.<br/>فالرأس يتسع في ذاكرة تكاد لا تُذكر،<br/>والذيل غير قابل للتخزين بطبيعته."]
        I2["سياسة القبول أهم من سياسة الإخراج:<br/>لا تقبل استعلامًا من أول ظهور:<br/>فأغلب استعلامات الذيل تُرى مرة واحدة بالضبط."]
    end

    classDef head fill:#fecaca,stroke:#b91c1c,color:#1c1917
    classDef torso fill:#fed7aa,stroke:#c2410c,color:#1c1917
    classDef tail fill:#dbeafe,stroke:#1e40af,color:#1c1917
    classDef good fill:#bbf7d0,stroke:#15803d,color:#1c1917
    class HEAD,HC head
    class TORSO,TC torso
    class TAIL,TLC tail
    class SAVE good
```

<div dir="rtl" align="right">

**والصندوق الثاني هو المهم عمليًا.** فذاكرة LRU الساذجة تقبل كل استعلام من أول طلب. ولمّا كان ~١٥٪ من استعلامات اليوم لم تُرَ من قبل ومن الأرجح ألا تُرى ثانيةً، فإن الذاكرة الساذجة تنفق حصة كبيرة من سعتها على مدخلات لن تُقرأ أبدًا. واشتراط رؤية الاستعلام مرتين ضمن نافذة قبل قبوله (مرشّح قبول بأسلوب TinyLFU) قد يرفع نسبة الإصابة الفعلية رفعًا معتبرًا بلا ذاكرة إضافية.

---

## ١٠.٣ تراتبية الذاكرات المؤقتة

لا توجد ذاكرة واحدة، بل ست، في طبقات مختلفة، بمفاتيح وأحجام ومدد صلاحية مختلفة.

> **المخطط D-52 · تراتبية الذاكرات متعددة الطبقات**

</div>

```mermaid
flowchart TB
    U(("👤 المستخدم"))

    U --> L1["① ذاكرة المتصفح<br/>المفتاح: الرابط الكامل<br/>الصلاحية: ثوانٍ إلى دقائق<br/>توفّر: الرحلة كاملة"]
    L1 -->|"إخفاق"| L2["② ذاكرة الحافة / CDN<br/>الأصول الساكنة والسكربتات والأنماط<br/>وليس النتائج المخصَّصة<br/>توفّر: رحلة إلى المصدر"]

    L2 -->|"إخفاق"| L3["③ ذاكرة النتائج<br/>المفتاح: استعلام موحَّد + محلية +<br/>دولة + بحث آمن + صنف جهاز<br/>القيمة: مجموعة النتائج المرتبة كاملة<br/>الصلاحية: 5 دقائق إلى 24 ساعة<br/>الإصابة ~40٪ · وهي الكبرى"]

    L3 -->|"إخفاق"| L4["④ ذاكرة فهم الاستعلام<br/>المفتاح: السلسلة الخام<br/>القيمة: الإملاء والنية والتوسيع والتضمين<br/>الصلاحية: ساعات<br/>الإصابة ~60٪ · رخيصة وفعّالة جدًا"]

    L4 --> L5["⑤ ذاكرة قوائم الترحيل<br/>المفتاح: معرّف المصطلح<br/>القيمة: كتل ترحيل ساخنة مفكوكة<br/>على خادم الأوراق، بسياسة LFU<br/>الإصابة ~85٪ للمصطلحات الشائعة"]

    L5 --> L6["⑥ ذاكرة الوثائق والمقتطفات<br/>المفتاح: معرّف الوثيقة + مصطلحات الاستعلام<br/>القيمة: المقتطف المعروض والعنوان<br/>تحمي مخزن الوثائق من<br/>تضخيم القراءة في الفصل ٠٧"]

    L6 --> IDX[("الفهرس ومخزن الوثائق")]

    subgraph TTL["الصلاحية حسب نية الاستعلام"]
        T1["ملاحية 'فيسبوك' ← 24 ساعة<br/>الجواب لا يتغير عمليًا"]
        T2["معلوماتية ← 1 إلى 6 ساعات"]
        T3["محلية ← ساعة<br/>المواعيد والتوفر تتغير"]
        T4["إخبارية ← 1 إلى 5 دقائق"]
        T5["رائجة أو عاجلة ← 30 ثانية أو تجاوز"]
        T6["مخصَّصة ← لا تُخزَّن عالميًا"]
    end

    L3 -.تحكمها.-> TTL

    classDef client fill:#e5e7eb,stroke:#4b5563,color:#1c1917
    classDef big fill:#bbf7d0,stroke:#15803d,color:#1c1917
    classDef server fill:#bfdbfe,stroke:#1d4ed8,color:#1c1917
    class L1,L2 client
    class L3,L4 big
    class L5,L6 server
```

<div dir="rtl" align="right">

### مفتاح الذاكرة هو أصعب ما في الأمر

كل بُعد يُضاف إلى المفتاح يضاعف فضاء المفاتيح ويقسّم نسبة الإصابة:

| المفتاح | المفاتيح المتمايزة تقريبًا | الأثر على الإصابة |
|---|---|---|
| `الاستعلام` | 10⁹ | خط الأساس |
| `+ الدولة` | × 200 | ضروري: النتائج تختلف فعلًا |
| `+ اللغة` | × 50 | ضروري |
| `+ البحث الآمن` | × 2 | ضروري: مسألة صحة |
| `+ صنف الجهاز` | × 3 | مبرَّر: التخطيط يختلف |
| `+ المدينة` | × 10⁴ | ❌ يدمّر الذاكرة |
| `+ معرّف المستخدم` | × 10⁹ | ❌ تصير الذاكرة بلا جدوى |

وهذه هي الآلية الملموسة خلف التحذير في [الفصل ٠٨](08-ranking.md): **التخصيص مكلف لأنه يدمّر قابلية التخزين المؤقت**، لا لأن التخصيص صعب حسابيًا. والحل القياسي **تصميم من مرحلتين**: خزّن مجموعة النتائج المحسوبة عالميًا تحت مفتاح خشن، ثم طبّق إعادة ترتيب خفيفة لكل مستخدم بعد القراءة. فالـ200 مللي ثانية المكلفة من الاسترجاع والترتيب مشتركة، ولا يخصّ المستخدمَ إلا التعديل النهائي الرخيص.

---

## ١٠.٤ الإبطال

> **المخطط D-53 · تماسك الذاكرة ومسارات الإبطال**

</div>

```mermaid
flowchart TB
    subgraph TRIGGERS["ما يُبطل صفحة نتائج مخزّنة"]
        E1["نشر نسخة فهرس جديدة"]
        E2["إزالة وثيقة: قانونية أو سياسية"]
        E3["رصد خبر عاجل في موضوع"]
        E4["إطلاق نموذج ترتيب"]
        E5["انتهاء الصلاحية الطبيعي"]
        E6["تطبيق خفض رتبة للسبام"]
    end

    E1 --> S1{"الاستراتيجية"}
    E2 --> S2{"الاستراتيجية"}
    E3 --> S3{"الاستراتيجية"}
    E4 --> S1
    E5 --> S4{"الاستراتيجية"}
    E6 --> S2

    S1 --> V["🏷️ مفاتيح موسومة بالنسخة<br/>يتضمن المفتاح نسخة الفهرس<br/>فنشر نسخة جديدة يجعل<br/>كل مدخلة قديمة غير قابلة للوصول:<br/>بلا حاجة إلى تطهير صريح"]

    S2 --> P["📢 بثّ تطهير نشط<br/>معرّف الوثيقة ← طبقة الذاكرة<br/>يجب أن يكون سريعًا وموثوقًا<br/>فهذا مسار الامتثال القانوني"]

    S3 --> T["⏱️ انهيار الصلاحية<br/>خفّض صلاحية الموضوع المتأثر<br/>من ساعات إلى ثوانٍ"]

    S4 --> L["🔄 انتهاء كسول + تحديث استباقي<br/>قدّم المتقادم بينما<br/>تُعيد الحساب لاتزامنيًا"]

    V & P & T & L --> RISK{"فحص المخاطر"}

    RISK --> R1["⚠️ اندفاع القطيع:<br/>إن انتهت كل المدخلات معًا<br/>ضربت طبقةَ الفهرس<br/>موجةٌ متزامنة"]
    R1 --> F1["✅ الحل: تشويش عشوائي للصلاحية<br/>+ دمج الطلبات<br/>(إعادة حساب واحدة لكل مفتاح)"]

    RISK --> R2["⚠️ ذاكرة باردة بعد النشر:<br/>طبقة أُعيد تشغيلها إصابتها 0٪<br/>وقد تُغرق طبقة الفهرس"]
    R2 --> F2["✅ الحل: إعادة تشغيل على مراحل<br/>وتسخين من لقطة دائمة<br/>وحدود معدل للقبول"]

    RISK --> R3["⚠️ تقديم محتوى مُزال:<br/>التطهير الذي يفشل صامتًا<br/>إخفاق امتثال لا علة أداء"]
    R3 --> F3["✅ الحل: إقرارات التطهير<br/>وقائمة منع تُفحص وقت القراءة<br/>كخط أخير مع سجل تدقيق"]

    classDef strat fill:#bfdbfe,stroke:#1d4ed8,color:#1c1917
    classDef risk fill:#fecaca,stroke:#b91c1c,color:#1c1917
    classDef fix fill:#bbf7d0,stroke:#15803d,color:#1c1917
    class V,P,T,L strat
    class R1,R2,R3 risk
    class F1,F2,F3 fix
```

<div dir="rtl" align="right">

**المفاتيح الموسومة بالنسخة هي الحل الأنيق وينبغي أن تكون الافتراضي.** فلمّا كانت مقاطع الفهرس ثابتة ([الفصل ٠٦](06-indexing.md))، صار للفهرس الحي رقم نسخة. وتضمينه في مفتاح الذاكرة يعني أن نشر فهرس جديد يُبطل الذاكرة كلها ذرّيًا دون إرسال رسالة تطهير واحدة: إذ تصير المدخلات القديمة غير قابلة للوصول وتتقادم طبيعيًا. وهكذا يؤتي الثبات ثماره للمرة الثالثة.

**لكن الوسم بالنسخة لا يكفي للإزالات القانونية.** فهذه لا تستطيع انتظار النشر التالي للفهرس، بل تحتاج مسار التطهير النشط **مع** قائمة منع تُفحص وقت القراءة كخط أخير، لأن رسالة تطهير تُسقط صامتةً تنتج انتهاكًا للامتثال لا نتيجة متقادمة. وهذه حالة يستحق فيها الدفاع المتعدد الطبقات ما يستحق: فالمسار السريع هو التطهير، وضمانة الصحة هي الفحص وقت القراءة.

---

## ١٠.٥ ما لا يجوز تخزينه أبدًا

| البيانات | السبب |
|---|---|
| ترتيب النتائج المخصَّص | تقديم نتائج مستخدم لآخر حادثة خصوصية |
| المحتوى المُصادق عليه والخاص | لا يدخل الفهرس العام أصلًا |
| الروابط المُزالة قانونيًا | يجب أن تختفي فورًا وبشكل قابل للتحقق |
| البيانات اللحظية (النتائج المباشرة، الأسعار) | التقادم هنا هو الفشل نفسه لا تحسين |
| أي شيء مفتاحه هوية مستخدم جزئية | يصير تسميم الذاكرة تسريبًا للبيانات بين المستخدمين |

**وتصادم مفاتيح الذاكرة ثغرة أمنية لا علة برمجية.** فإن أمكن لطلبين منطقيين مختلفين أن يُسقطا على المفتاح نفسه، أمكن لمستخدم أن يتلقى نتائج آخر. ولهذا يجب أن يتضمن المفتاح **كل** بُعد يؤثر في الاستجابة: بما فيها حالة البحث الآمن والدولة، وهما بُعدا صحة لا أداء. والمبدأ الآمن: **عند الشك، أضف البُعد إلى المفتاح واقبل نسبة إصابة أدنى.**

---

## ١٠.٦ قياس فعالية الذاكرة

> **المخطط D-54 · المقاييس التي تهم فعلًا**

</div>

```mermaid
flowchart LR
    subgraph GOOD["✅ مقاييس تستحق التحسين"]
        G1["نسبة الإصابة لكل طبقة<br/>ولكل شريحة استعلامات"]
        G2["التكلفة المتجنَّبة<br/>= الإصابات × كلفة استعلام الفهرس<br/>وهو المقياس: لأنه مال"]
        G3["توزيع التقادم<br/>عمر المدخلات المقدَّمة عند p50 وp99"]
        G4["خفض استعلامات المصدر"]
        G5["حوادث الصحة<br/>يجب أن تكون صفرًا"]
    end

    subgraph BAD["⚠️ مقاييس مضلِّلة"]
        B1["نسبة الإصابة وحدها<br/>← قابلة للتلاعب بتخزين<br/>استعلامات رخيصة<br/>وإطالة الصلاحيات"]
        B2["زمن الذاكرة<br/>← هو ~1 مللي ثانية أصلًا<br/>وتحسينه لا يغيّر شيئًا"]
        B3["استغلال الذاكرة<br/>← الذاكرة الممتلئة ليست<br/>بالضرورة فعّالة"]
    end

    G2 --> DECIDE{"القرار"}
    DECIDE --> D1["أضف ذاكرة إن كانت<br/>التكلفة الحدية المتجنَّبة ><br/>التكلفة الحدية للذاكرة"]
    DECIDE --> D2["أطل الصلاحية فقط إن بقي<br/>التقادم ضمن تحمّل النية"]
    DECIDE --> D3["حسّن سياسة القبول<br/>وهي عادةً أرخص مكسب"]

    classDef good fill:#bbf7d0,stroke:#15803d,color:#1c1917
    classDef bad fill:#fecaca,stroke:#b91c1c,color:#1c1917
    class G1,G2,G3,G4,G5 good
    class B1,B2,B3 bad
```

<div dir="rtl" align="right">

**نسبة الإصابة وحدها فخ.** إذ يمكنك رفعها بسهولة بإطالة مدد الصلاحية، وهذا يقايض تحسّن مقياس بنتائج أكثر تقادمًا صامتًا. والمقياس غير القابل للتلاعب هو **التكلفة المتجنَّبة لكل وحدة تقادم مُدخَل**، ويجب تقييمه دائمًا إلى جانب عدد حوادث الصحة، وهدفه الصارم صفر.

</div>

<div align="center">

**← [٠٩ · التخزين](09-storage.md)** · [🏠 الرئيسية](../../README.ar.md) · **[١١ · الحداثة](11-freshness.md) →** · [🇬🇧 English](../en/10-caching.md)

</div>
