<div align="left">

**[🇬🇧 Read in English](../en/11-freshness.md)**

</div>

<div dir="rtl" align="right">

# الفصل ١١ · الحداثة والفهرسة اللحظية

**← [١٠ · التخزين المؤقت](10-caching.md)** · [🏠 الرئيسية](../../README.ar.md) · **[١٢ · الموثوقية](12-reliability.md) →**

---

## ١١.١ المشكلة التي لا تحلّها الفهرسة الدفعية

وصف [الفصل ٠٦](06-indexing.md) بناء الفهرس بأنه عملية MapReduce على المتن كله: بيتابايت من النص و~١٤ ساعة. وهذا كافٍ تمامًا للويب العادي، لكنه عديم الفائدة تمامًا عند وقوع زلزال.

المتطلب من [الفصل ٠٢](02-requirements.md) هو **وسيط زمن فهرسة أقل من ٥ دقائق للمجموعة الساخنة**. ولا تستطيع إعادة البناء الكاملة تحقيق ذلك بأي ثمن، لأن عنق الزجاجة ليس الحوسبة، بل كون إعادة البناء الكاملة **تعيد حساب 10¹¹ وثيقة لتعكس تغيّرًا في 10⁶ منها**. فتضخيم الكتابة ١٠٠٬٠٠٠ ضعف.

والجواب ليس تسريع المهمة الدفعية، بل **التوقف عن إعادة بناء ما لم يتغيّر**، وهذه بالضبط بصيرة ورقة Percolator من جوجل (٢٠١٠) ونظام الفهرسة Caffeine الذي أتاحته.

> **المخطط D-55 · الفهرسة ثنائية المسار**

</div>

```mermaid
flowchart TB
    WEB["🌐 تغيّرات الويب"]

    WEB --> DETECT{"كيف عُرف<br/>التغيّر؟"}

    DETECT -->|"دفع: خلاصة أو تنبيه<br/>خريطة موقع أو واجهة برمجية"| FAST
    DETECT -->|"سحب: إعادة زحف مجدولة<br/>وجدت محتوى جديدًا"| CLASSIFY
    DETECT -->|"إشارة: استعلام رائج<br/>بلا نتائج جيدة"| FAST

    CLASSIFY{"هل الصفحة<br/>حرجة الحداثة؟"} -->|"نعم: أخبار ومنتديات<br/>ومعدل تغير عالٍ<br/>وطلب استعلامات عالٍ"| FAST
    CLASSIFY -->|"لا: الـ99.9٪ الباقية"| SLOW

    subgraph FAST["⚡ المسار السريع: تدفقي · ثوانٍ إلى دقائق"]
        F1["جلب بأولوية<br/>يتجاوز الطابور العادي"]
        F2["معالجة تدفقية<br/>تحليل · إزالة تكرار · تصنيف"]
        F3["حساب إشارات تزايدي<br/>بيج رانك تقريبي<br/>ونصوص روابط جزئية"]
        F4["كتابة في مقطع فهرس<br/>لحظي في الذاكرة"]
        F5["قابل للبحث فورًا"]
        F1 --> F2 --> F3 --> F4 --> F5
    end

    subgraph SLOW["🐢 المسار الدفعي: ساعات إلى أيام"]
        S1["طابور الزحف العادي"]
        S2["معالجة دفعية"]
        S3["إشارات كاملة الدقة<br/>بيج رانك دقيق على<br/>البيان الكامل"]
        S4["بناء الفهرس بـMapReduce"]
        S5["نشر المقاطع"]
        S1 --> S2 --> S3 --> S4 --> S5
    end

    F5 --> SERVE
    S5 --> SERVE

    subgraph SERVE["🔍 طبقة الخدمة: تستعلم كليهما وتدمج"]
        M1[("مقاطع لحظية<br/>صغيرة · في الذاكرة · عمرها دقائق")]
        M2[("مقاطع دفعية<br/>كبيرة · ثابتة · عمرها ساعات وأيام")]
        M3["الاستعلام يوزَّع على الاثنين<br/>وتُدمج النتائج ويُزال تكرارها"]
        M1 --> M3
        M2 --> M3
    end

    F5 -.تحلّ محلها لاحقًا.-> S5

    classDef fast fill:#fecaca,stroke:#b91c1c,color:#1c1917
    classDef slow fill:#dbeafe,stroke:#1e40af,color:#1c1917
    classDef serve fill:#bbf7d0,stroke:#15803d,color:#1c1917
    class F1,F2,F3,F4,F5 fast
    class S1,S2,S3,S4,S5 slow
    class M1,M2,M3 serve
```

<div dir="rtl" align="right">

**الفكرة البنيوية الأساسية: المسار السريع يقايض الدقة بالزمن، والمسار الدفعي يصحّحه لاحقًا.** فالوثيقة المفهرسة عبر المسار السريع تحمل إشارات ترتيب تقريبية؛ بيج رانك محسوبًا من بيان جزئي، ونصوص روابط مما تصادف معرفته. فهي **قابلة للإيجاد** خلال دقائق لكنها ليست **مرتَّبة** بدقة. وحين يلحق الخط الدفعي بعد ساعات، تحلّ النسخة الدقيقة محلها. فينال المستخدمون جوابًا سريعًا خاطئًا قليلًا الآن بدل جواب مثالي غدًا، وهذه في الأخبار العاجلة مقايضة صحيحة بلا لبس.

---

## ١١.٢ المعالجة التزايدية: نموذج Percolator

المعالجة الدفعية تعيد حساب كل شيء. أما التزايدية فتحسب ما تغيّر فقط وتنشر عواقبه. والآلية هي **المراقبون**: مُطلِقات تنطلق حين يتغير عمود محدد في صف محدد.

> **المخطط D-56 · الفهرسة التزايدية المقادة بالمراقبين**

</div>

```mermaid
flowchart TB
    W["كتابة: الزاحف يخزّن<br/>محتوى جديدًا للرابط س"]

    W --> T1[("جدول الزحف<br/>الصف: com.example/X<br/>العمود: المحتوى ← جديد")]

    T1 -->|"يُطلق"| O1["👁️ مراقب: تغيّرت الوثيقة"]

    O1 --> A1["إعادة حساب بصمة المحتوى وSimHash"]
    A1 --> D{"هل يختلف فعلًا<br/>عن النسخة السابقة؟"}
    D -->|"لا: 304 أو مطابق"| STOP["🛑 توقّف.<br/>لا عمل لاحقًا.<br/>هذا الخروج المبكر هو ما<br/>يجعل التزايدية قابلة للتطبيق."]
    D -->|"نعم"| A2["إعادة تحليل: نص وروابط وبيانات وصفية"]

    A2 --> T2[("كتابة: النص والبيانات<br/>الوصفية والروابط الصادرة")]

    T2 -->|"يُطلق"| O2["👁️ مراقب: تغيّرت الروابط"]
    O2 --> A3["مفاضلة مجموعتي الروابط القديمة والجديدة"]
    A3 --> A4["لكل رابط مُضاف ← اكتب<br/>سجل رابط وارد على صف الهدف"]
    A3 --> A5["لكل رابط محذوف ← احذف<br/>سجل الرابط الوارد من صف الهدف"]

    A4 & A5 --> T3[("تحديث صفوف بيان الروابط<br/>على وثائق أخرى")]

    T3 -->|"يُطلق"| O3["👁️ مراقب: تغيّرت الروابط الواردة"]
    O3 --> A6["تحديث نصوص الروابط المجمّعة"]
    A6 --> A7["وسم بيج رانك الهدف كمتقادم<br/>⚠️ لا تعِد حسابه عالميًا:<br/>بل أدرجه للتمريرة الدفعية التالية"]

    T2 -->|"يُطلق"| O4["👁️ مراقب: جاهز للفهرسة"]
    O4 --> A8["إصدار تحديث الفهرس:<br/>المعرّف والرموز والمواضع والإشارات"]
    A8 --> T4[("مقطع فهرس لحظي")]

    T4 --> SERVE(("قابل للبحث<br/>خلال دقائق"))

    subgraph TX["لماذا يحتاج هذا معاملات"]
        X1["المراقب يقرأ عدة صفوف<br/>ويكتب عدة صفوف."]
        X2["مراقبان ينطلقان متزامنين<br/>على صف الهدف نفسه<br/>سيُفسدان مجموعة الروابط الواردة."]
        X3["← لذا يضيف Percolator معاملات<br/>عزل لقطي متعددة الصفوف<br/>فوق مخزن الأعمدة العريضة،<br/>باستخدام عرّاف طوابع زمنية."]
        X1 --> X2 --> X3
    end

    O2 -.يتطلب.-> TX

    classDef obs fill:#fde68a,stroke:#b45309,color:#1c1917
    classDef store fill:#e9d5ff,stroke:#7e22ce,color:#1c1917
    classDef stop fill:#bbf7d0,stroke:#15803d,color:#1c1917
    class O1,O2,O3,O4 obs
    class T1,T2,T3,T4 store
    class STOP stop
```

<div dir="rtl" align="right">

### لماذا هذا صعب فعلًا

مخزن الأعمدة العريضة من [الفصل ٠٩](09-storage.md) يقدّم عمدًا **ذرّية صف واحد فقط**. لكن المراقب الذي يضيف رابطًا واردًا يجب أن يقرأ صف الهدف ويعدّله ويكتبه ثانيةً، ومراقبان يفعلان ذلك متزامنين على هدف شائع سيفقدان تحديثات.

وكانت مساهمة Percolator إضافة **معاملات عزل لقطي متعددة الصفوف فوق** مخزن من صنف Bigtable، عبر عرّاف طوابع زمنية مركزي وتثبيت ثنائي المرحلة مُرمَّز في أعمدة إضافية. والكلفة معتبرة: فالكتابة المعاملاتية أغلى بأضعاف من العادية. والفائدة أن الفهرسة التزايدية تصير **صحيحة**، وهذا ما يجعلها صالحة للإنتاج بدل أن تكون مصدرًا لبيانات روابط فاسدة بخفاء.

**وعقدة `توقّف` تستحق الانتباه أيضًا.** فالخروج المبكر عند عدم تغيّر المحتوى ليس تفصيلة تحسين، بل هو الخاصية التي تجعل المقاربة كلها قابلة للتطبيق. إذ إن أغلب عمليات إعادة الزحف لا تجد جديدًا ([الفصل ٠٤](04-crawling.md))، فتنتهي أغلب سلاسل المراقبين عند الخطوة الأولى. وبدون ذلك الخروج، لأدّت المعالجة التزايدية عملًا بحجم الدفعي وبتواتر التدفقي.

---

## ١١.٣ الاكتشاف: الدفع يتفوق على السحب في الحداثة

انتظار **اكتشاف** التغيّر بإعادة الزحف أبطأ مسار ممكن. والناشرون الراغبون في فهرسة سريعة يستطيعون إخبارك مباشرة.

> **المخطط D-57 · قنوات اكتشاف التغيّر مرتَّبة بالزمن**

</div>

```mermaid
flowchart LR
    subgraph PUSH["📤 الدفع: يبدأه الناشر · ثوانٍ"]
        P1["تقديم عبر واجهة الفهرسة<br/>الزمن: ثوانٍ<br/>⚠️ يتطلب تحقق ملكية الموقع<br/>وحصصًا صارمة، وعرضة للإساءة"]
        P2["تنبيه خريطة الموقع عند التحديث<br/>الزمن: ثوانٍ إلى دقائق"]
        P3["دفع الخلاصات اللحظي<br/>WebSub"]
        P4["استطلاع RSS و Atom<br/>الزمن: دقائق"]
    end

    subgraph SIGNAL["📡 مقادة بالإشارات · دقائق"]
        S1["ارتفاع طلب الاستعلامات:<br/>مستخدمون كثر يبحثون عن مصطلح<br/>بلا نتائج جيدة<br/>← زحف مستهدف"]
        S2["ارتفاع حجم الإشارات<br/>الاجتماعية والخارجية"]
        S3["صفحة عالية السلطة<br/>ترتبط بشيء جديد"]
    end

    subgraph PULL["📥 السحب: يبدأه الزاحف · دقائق إلى أسابيع"]
        L1["إعادة زحف عالية التواتر<br/>للصفحات الساخنة المعروفة<br/>الزمن: دقائق"]
        L2["إعادة قراءة خرائط المواقع دوريًا<br/>الزمن: ساعات"]
        L3["إعادة الزحف المجدولة العادية<br/>الزمن: أيام إلى أسابيع"]
        L4["اكتشاف الروابط من<br/>صفحات مزحوفة أخرى<br/>الزمن: غير محدود"]
    end

    P1 & P2 & P3 & P4 --> VERIFY["طبقة التحقق<br/>⚠️ قنوات الدفع يتحكم بها المهاجم"]
    S1 & S2 & S3 --> VERIFY
    L1 & L2 & L3 & L4 --> VERIFY

    VERIFY --> V1["تأكيد ملكية الموقع"]
    VERIFY --> V2["فرض حصص لكل موقع"]
    VERIFY --> V3["اجلب وتحقق أن المحتوى<br/>تغيّر فعلًا: لا تثق بالادعاء وحده"]
    VERIFY --> V4["طبّق تسجيل السبام والجودة<br/>قبل القبول في المسار السريع"]

    V1 & V2 & V3 & V4 --> FT["طابور المسار السريع"]

    classDef push fill:#bbf7d0,stroke:#15803d,color:#1c1917
    classDef sig fill:#fde68a,stroke:#b45309,color:#1c1917
    classDef pull fill:#dbeafe,stroke:#1e40af,color:#1c1917
    classDef sec fill:#fecaca,stroke:#b91c1c,color:#1c1917
    class P1,P2,P3,P4 push
    class S1,S2,S3 sig
    class L1,L2,L3,L4 pull
    class VERIFY,V1,V2,V3,V4 sec
```

<div dir="rtl" align="right">

**كل قناة دفع سطح هجوم.** فواجهة «أخبرني حين تُحدِّث» هي (من منظور المُسيء), وسيلة لتخطي طابور الزحف. ولذا فطبقة التحقق ليست توصيلًا اختياريًا: يجب إثبات الملكية، وفرض الحصص لكل موقع مُتحقَّق منه، وتأكيد التغيّر المُدَّعى بجلب الصفحة فعلًا، وتشغيل تسجيل الجودة **قبل** دخول الوثيقة المسار السريع لا بعده. فالمسار السريع بلا بوابة تحقق ليس إلا مسارًا سريعًا للسبام.

**والقناة المقادة بالإشارات هي الذكية.** فإن بحث آلاف المستخدمين فجأةً عن مصطلح ولم يجد الفهرس نتائج جيدة، فذلك دليل مباشر على أن المتن تنقصه أشياء مهمة الآن. وتحويل الطلب غير الملبَّى إلى أولوية زحف يُغلق الحلقة من [المخطط D-01](../../README.ar.md) (سلوك المستخدم يوجّه الزاحف), ويلتقط أحداثًا لم يفكر أي ناشر في الإعلان عنها.

---

## ١١.٤ خدمة متن مقسوم على فهرسين

يجب أن تستعلم طبقة الخدمة المقاطع الدفعية واللحظية معًا وتنتج ترتيبًا متماسكًا واحدًا.

> **المخطط D-58 · دمج النتائج اللحظية والدفعية**

</div>

```mermaid
sequenceDiagram
    autonumber
    participant R as الجذر
    participant L as خادم الأوراق
    participant B as المقاطع الدفعية
    participant RT as المقاطع اللحظية
    participant M as منطق الدمج

    R->>L: استعلام وأفضل k
    par البحث في مجموعتي المقاطع
        L->>B: اجتياز المدخلات الدفعية<br/>(كبيرة ومضغوطة وثابتة)
        L->>RT: اجتياز المدخلات اللحظية<br/>(صغيرة في الذاكرة وحديثة)
    end
    B-->>L: مرشحون بإشارات كاملة الدقة
    RT-->>L: مرشحون بإشارات تقريبية

    L->>M: ادمج

    M->>M: 1️⃣ إزالة التكرار بالمعرّف<br/>قد توجد الوثيقة في الاثنين:<br/>واللحظية أحدث
    Note over M: القاعدة: الأحدث يفوز.<br/>المقاطع الدفعية تحمل طابعًا<br/>مرجعيًا، والمدخلات اللحظية<br/>الأقدم منه تُهمل.

    M->>M: 2️⃣ توفيق مقاييس الدرجات<br/>⚠️ بيج رانك التقريبي غير<br/>قابل للمقارنة بالدقيق
    Note over M: الحل: عايِر الإشارات اللحظية<br/>مقابل التوزيع الدفعي، أو طبّق<br/>أولوية حداثة صريحة بدل ادّعاء<br/>تكافؤ الدرجتين.

    M->>M: 3️⃣ تطبيق شواهد القبور<br/>الحذف من أي مصدر

    M->>M: 4️⃣ تطبيق تعزيز الحداثة<br/>فقط إن كانت نية الاستعلام حديثة

    M-->>R: أفضل k موحَّدة مع رايات التغطية

    Note over L,RT: الضغط مستمر:<br/>تُطوى المقاطع اللحظية داخل<br/>الدفعية وتُسقط، فتبقى المجموعة<br/>اللحظية صغيرة بما يكفي للذاكرة.
```

<div dir="rtl" align="right">

**الخطوة ٢ هي حيث تنكسر التنفيذات الساذجة.** فالوثيقة في الفهرس اللحظي لها بيج رانك تقريبي محسوب من بيان روابط جزئي، وهو عادةً **تقدير أدنى**، لأن أغلب روابطها الواردة لم تُكتشف بعد. وإن دمجت تلك الدرجات مباشرةً مع الدرجات الدفعية الدقيقة، عوقبت الوثائقُ الحديثة منهجيًا، وأنتج خط الحداثة المكلف محتوًى لا يراه أحد.

والمعالجة الصحيحة أن تعامل التوزيعين بوصفهما مختلفين وتعايِر بينهما، أو (وهو الأمتن) أن تُبقي الحداثة **أولوية ترتيب صريحة** يطبّقها المُرتِّب لاستعلامات النية الحديثة، بدل أن تأمل انبثاقها ضمنيًا من إشارات مختلطة الدقة.

---

## ١١.٥ مقايضة الحداثة بالتكلفة

الحداثة ليست مجانية، وتكلفتها ليست موزعة بالتساوي.

| شريحة المتن | النسبة | فترة إعادة الزحف | المسار | التكلفة النسبية للوثيقة |
|---|---:|---|---|---:|
| الأخبار العاجلة والبث المباشر | ~0.001٪ | ثوانٍ إلى دقائق | دفع + مسار سريع | **~١٠٠٠×** |
| مواقع الأخبار النشطة والمنتديات | ~0.1٪ | دقائق إلى ساعات | مسار سريع | ~١٠٠× |
| المواقع كثيرة التحديث | ~1٪ | ساعات إلى يوميًا | مختلط | ~١٠× |
| صفحات الويب العادية | ~20٪ | أسبوعيًا إلى شهريًا | دفعي | ١× |
| الأرشيفات الساكنة وملفات PDF | ~79٪ | فصليًا أو أبدًا | دفعي منخفض الأولوية | ~0.1× |

**وتركيز كل إنفاق الحداثة تقريبًا على ~٠.١٪ من المتن هو الاستراتيجية بأكملها.** فتطبيق المسار السريع بالتساوي سيضاعف كلفة الفهرسة برتبتَي مقدار تقريبًا بينما يحسّن النتائج لأحد يكاد لا يُذكر، لأن الـ٩٩.٩٪ الأخرى من الوثائق لم تكن ستتغير أصلًا.

وهذا الشكل نفسه هو قرار الطبقات في [الفصل ٠٦](06-indexing.md) وقرار التخزين المؤقت في [الفصل ١٠](10-caching.md)، ويستحق التسمية الصريحة كنمط متكرر:

> **الأنظمة بحجم الويب تصير ميسورة باستغلال الانحراف الشديد. حدّد الجزء الصغير الذي يهم، وأنفق عليه بسخاء، وعامل الباقي بثمن بخس وبالجملة.**

وكل قرار تكلفة كبير في هذا المستودع هو تجسيد لتلك الفكرة الواحدة.

</div>

<div align="center">

**← [١٠ · التخزين المؤقت](10-caching.md)** · [🏠 الرئيسية](../../README.ar.md) · **[١٢ · الموثوقية](12-reliability.md) →** · [🇬🇧 English](../en/11-freshness.md)

</div>
